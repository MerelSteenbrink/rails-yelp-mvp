<h1>Create a new Review</h1>

<%= form_for @review, :url => restaurant_reviews_path(@restaurant.id) do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.label :rating %>
  <%= f.text_field :rating %>

  <%= f.submit %>
<% end %>
